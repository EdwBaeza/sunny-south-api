name: Django CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: build docker stack
      run: pwd
      run: ls -la
      run: ls -la sunnysouth
      run: docker-compose -f local.yml build
    - name: Run Tests
      run: docker-compose -f local.yml run --rm django pytest
