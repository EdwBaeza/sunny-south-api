name: Django CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Verbose directories
      run: |
        pwd
        ls -la
        ls -la sunnysouth
    - name: build docker stack
      run: docker-compose -f local.yml build
    - name: Run Tests
      run: docker-compose -f local.yml run --rm django pytest
